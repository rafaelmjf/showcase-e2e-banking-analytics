name: banking_analytics
version: 1.0.0
config-version: 2

profile: banking_analytics

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  banking_analytics:
    +materialized: table
    staging:
      +schema: staging
      +tags: ["fixture_ready", "staging"]
    core:
      +schema: core
      +tags: ["fixture_ready", "core"]
